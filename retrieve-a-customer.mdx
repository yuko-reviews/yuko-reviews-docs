---
title: "Retrieve a customer"
---

Retrieves a single customer by UUID.

**GET /customers/\{uuid\}**

#### **Authorizations**

**Authorization**

**string**

**header**

**required**

Bearer authentication header of the form `Bearer <token>`, where `<token>` is your auth token.

#### **Path Parameters**

**uuid**

**string\<uuid\>**

**required**

The UUID of the customer in Yuko to retrieve.

Example:

`"63a2c89a-9936-4bc0-9d5f-93db46110b76"`

#### **Response**

**200 - application/json**

The customer was successfully retrieved.

**customer**

**object**

Hide child attributes

**customer.id**

**integer**

Unique integer identifier for the customer in Yuko.

Example:

`1379409`

**customer.uuid**

**string\<uuid\>**

Unique UUID identifier for the customer in Yuko.

Example:

`"63a2c89a-9936-4bc0-9d5f-93db46110b76"`

**customer.platform**

**string**

The ecommerce platform the customer was synced from.

Example:

`"woocommerce"`

**customer.platform_customer_id**

**string | null**

The customer's unique identifier in the external platform.

Example:

`"cust_001"`

**customer.org_uuid**

**string\<uuid\>**

The UUID of the organisation this customer belongs to.

Example:

`"7789dd6a-5f90-4ad1-9126-63ada4827f79"`

**customer.first_name**

**string | null**

The customer's first name.

Example:

`"Bryn"`

**customer.last_name**

**string | null**

The customer's last name.

Example:

`"Johnson"`

**customer.email**

**string**

The customer's email address.

Example:

`"brynjohnson1@outlook.com"`

**customer.phone**

**string | null**

The customer's phone number.

Example:

`"07507355101"`

**customer.birth_date**

**string\<date\> | null**

The customer's date of birth.

Example:

`"1990-05-27"`

**customer.company**

**string | null**

The customer's company name.

Example:

`"Acme Ltd"`

**customer.address**

**string | null**

The customer's primary address line.

Example:

`"12 High Street"`

**customer.address2**

**string | null**

The customer's secondary address line.

Example:

`"Apartment 4"`

**customer.city**

**string | null**

The customer's city.

Example:

`"Penryn"`

**customer.province**

**string | null**

The customer's province or county.

Example:

`"Cornwall"`

**customer.province_code**

**string | null**

The short code for the customer's province or county.

Example:

`"cornwall"`

**customer.zip**

**string | null**

The customer's postal or zip code.

Example:

`"TR10 9FE"`

**customer.country**

**string | null**

The customer's country name.

Example:

`"GB"`

**customer.country_code**

**string | null**

The ISO 3166-1 alpha-2 country code.

Example:

`"GB"`

**customer.accepts_marketing**

**boolean**

Whether the customer has opted in to marketing communications.

Example:

`true`

**customer.is_email_subscribed**

**boolean**

Whether the customer is subscribed to email communications.

Example:

`true`

**customer.email_marketing_consent**

**object**

The customer's email marketing consent details.

Show child attributes

**customer.email_marketing_consent.marketingOptInLevel**

**string**

The opt-in level for email marketing.

Example:

`"SINGLE_OPT_IN"`

**customer.email_marketing_consent.marketingState**

**string**

The current marketing state for email.

Example:

`"SUBSCRIBED"`

**customer.email_marketing_consent.marketingUpdatedAt**

**string\<date-time\>**

The date and time the email marketing consent was last updated.

Example:

`"2026-03-09T14:02:06.183Z"`

**customer.sms_marketing_consent**

**object**

The customer's SMS marketing consent details.

Show child attributes

**customer.sms_marketing_consent.marketingOptInLevel**

**string**

The opt-in level for SMS marketing.

Example:

`"SINGLE_OPT_IN"`

**customer.sms_marketing_consent.marketingState**

**string**

The current marketing state for SMS.

Example:

`"SUBSCRIBED"`

**customer.sms_marketing_consent.marketingUpdatedAt**

**string\<date-time\>**

The date and time the SMS marketing consent was last updated.

Example:

`"2026-03-09T14:02:06.183Z"`

**customer.total_spent**

**number**

The total amount spent by the customer across all orders.

Example:

`150.00`

**customer.currency**

**string**

The ISO 4217 currency code for the customer's store.

Example:

`"GBP"`

**customer.total_points**

**integer**

The customer's current loyalty points balance.

Example:

`500`

**customer.current_tier**

**object | null**

The customer's current VIP tier. `null` if the customer has not reached any tier.

Show child attributes

**customer.current_tier.uuid**

**string\<uuid\>**

Unique identifier for the VIP tier.

Example:

`"b3c4d5e6-f7a8-9012-bcde-f01234567890"`

**customer.current_tier.name**

**string**

The display name of the VIP tier.

Example:

`"Gold"`

**customer.membership**

**object**

Details about the customer's loyalty program membership.

Show child attributes

**customer.membership.status**

**string**

The customer's membership status in the loyalty program.

Available options:

`active`,

`inactive`,

`pending`

Example:

`"active"`

**customer.membership.joined_at**

**string\<date-time\> | null**

The date and time the customer joined the loyalty program.

Example:

`"2024-06-01T10:00:00.000Z"`

**customer.is_lp_banned_customer**

**string**

Whether the customer is banned from the loyalty program.

Available options:

`yes`,

`no`

Example:

`"no"`

**customer.platform_created_at**

**string\<date-time\>**

The date and time the customer was created in the external platform.

Example:

`"2026-03-09T14:02:06.000Z"`

**customer.platform_updated_at**

**string\<date-time\>**

The date and time the customer was last updated in the external platform.

Example:

`"2026-03-09T14:02:06.000Z"`

**customer.created_at**

**string\<date-time\>**

The date and time when the customer was created in Yuko.

Example:

`"2026-03-09T14:02:06.000Z"`

**customer.updated_at**

**string\<date-time\>**

The date and time when the customer was last updated in Yuko.

Example:

`"2026-03-09T14:02:06.000Z"`

**customer.deleted_at**

**string\<date-time\> | null**

The date and time when the customer was deleted. `null` if not deleted.

Example:

`null`

**customer.shopify_credit_account_id**

**string | null**

The Shopify credit account ID associated with the customer. Only applicable for Shopify stores.

Example:

`"gid://shopify/ShopifyPaymentsAccount/12345"`

**customer.tags**

**string[]**

A list of tags associated with the customer.

Example:

`["vip", "wholesale"]`
